<%= form_with(model: office) do |form| %>
  <% if office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
        <% office.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :building_id %>
    <%= form.select :building_id, options_from_collection_for_select(pbuildings, :id, :name, office.building_id),
    { include_blank: "select a building" } %>
    <% if office.errors[:building_id].any? %>
      <div>
        <%= office.errors[:building_id].first %>
      </div>
    <% end %>  

    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

